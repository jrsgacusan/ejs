<!doctype html>
<html>
  <head>
    <link href="/styles.css" rel="stylesheet" />
  </head>
  <body>
    <div class="flex gap-4 flex-wrap">
      <% if (items.length) {%> <% for (item of items) { %>
      <div>
        <%- include('./partials/card', {imageRef: item.imageRef, title: item.title, subtitle: item.subtitle}) %>
      </div>
      <% } %> <% } %>
    </div>
  </body>
</html>
